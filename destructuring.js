let person = {
    first: 'Joshua',
    last: 'Chukwuezi',
    city: 'Dublin',
    employer: 'SAP'
};

//let first = person.first
//console.log(first)

//you can use default values for destructuring too

//destructuring with objects

//updating person
//let {first, last, city, employer, friend='Jack Ryan'} = person;

//destructuring with arrays
let x = [1, 2, 3]

let [a,b,c] = x;

console.log(a);
console.log(b);
console.log(c);